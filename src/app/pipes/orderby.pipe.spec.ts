import { OrderBerryByPipe } from './orderby.pipe';

describe('OrderbyPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderBerryByPipe();
    expect(pipe).toBeTruthy();
  });
});
